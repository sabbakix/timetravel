# timetravel
Set file time

## Usage

Run the script from a terminal:

```bash
python main.py
```

Then enter:
- a file or directory path
- year, month, day, hour, minute, second

On Windows, creation time is also updated when `pywin32` is installed.
